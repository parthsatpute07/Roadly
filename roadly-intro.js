// =====================================
// ROADLY INTRO
// =====================================

window.addEventListener("load", () => {

    // Wait for the animation to finish
    setTimeout(() => {

        window.location.href = "index.html";

    }, 4500);

});
